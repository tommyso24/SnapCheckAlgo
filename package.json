{
  "name": "trade-check",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p ${PORT:-8080} -H 0.0.0.0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "next": "14.2.35",
    "react": "^18",
    "react-dom": "^18",
    "jose": "^5.6.3",
    "@upstash/redis": "^1.34.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
